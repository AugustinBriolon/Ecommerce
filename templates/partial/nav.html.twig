<nav id="navbar" class="navbar order-last order-lg-0">
    <ul>
        <li><a class="nav-link scrollto active" href="{{ path('app_index') }}">Home</a></li>
        <li class="dropdown"><a href="{{ path('app_productCategories') }}"><span>Product Categories</span> <i class="bi bi-chevron-down"></i></a>
            <ul>
                {% for productCategorie in productCategories %}
                    <li><a href="#">{{ productCategorie.name }}</a></li>
                {% endfor %}
            </ul>
        </li>
        <li class="dropdown"><a href="{{ path('app_products') }}"><span>Produits</span> <i class="bi bi-chevron-down"></i></a>
            <ul>
                {% for product in products %}
                    <li><a href="#">{{ product.name }}</a></li>
                {% endfor %}
            </ul>
        </li>
        {# {% if not user.roles %} #}
            <li><a class="getstarted scrollto" href="{{ path('app_login') }}">Log In</a></li>
        {# {% endif %} #}
    </ul>
    <i class="bi bi-list mobile-nav-toggle"></i>
</nav><!-- .navbar -->